# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TwitterPractice' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TwitterPractice
	pod 'FirebaseCore'
	pod 'FirebaseAuth'
	pod 'FirebaseStorage'
	pod 'FirebaseDatabase'
  pod 'SDWebImage', '~> 5.0'
  pod 'SwiftLint'
  pod 'ActiveLabel'
end
